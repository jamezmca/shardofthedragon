so i want a website with bascially no styles and all it has is at the top, two metrics. one is scroll depth, and the second is scroll duration. and depth is like the distance scrolled during the scroll duration, and scroll duration only goes up when a user is actively scrolling. nothing else appears on the screen, except for some random and very sparse darkened pixels which just show that we are in fact scrolling and give a perspective of the speed.

i want the scroll direction to be down. i want to competely remove the scroll bar, so the numbers are the only metric. i want the scroll depth saved to local storage.

i want the font to be inter. 

only white background and #0f172a for text color/pixel color.

the pixel spots are procedurally generated and we only need 2 / 3 across the entire page to give the appearance of scroll movement. 

website should obs work on mobile.

i want the two metrics to sit at top left and top right corners of website.

in terms of divs, i want the scrolling to be a javascript illusion, so there's not actually any scrolling going on, it's basically just moving the pixels from the bottom to the top of the page proportionally to the speed of the scrolling (do whatever you need to do to achieve that).

so someone might scroll slowly, and another fast so their duration is equal, but their depth wouldn't be.